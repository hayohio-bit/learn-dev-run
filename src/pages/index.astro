---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { Image } from "astro:assets";
import heroImage from "../assets/cloude_9.jpg";
---

<!doctype html>
<html lang="ko">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main class="home-main">
      <div class="hero-section">
        <Image
          src={heroImage}
          alt="Hero Image"
          class="hero-image"
          width={1200}
          height={600}
        />
        <div class="hero-overlay">
          <h1 class="hero-title">안녕하세요! 👋</h1>
          <p class="hero-subtitle">
            개발자로 성장하는 여정을 기록하는 공간입니다.
          </p>
        </div>
      </div>

      <div class="intro-section">
        <h2>블로그 소개</h2>
        <p>
          부트캠프에서 배운 내용과 개발하며 겪은 경험들을 기록합니다. 에러 해결
          과정, 새로 배운 개념, 프로젝트 회고 등을 담고 있어요.
        </p>
        <p>
          완벽하지 않아도 괜찮습니다. 오늘 이해한 만큼만, 나중의 제가 다시 봐도
          도움이 될 만한 내용을 천천히 쌓아가고 있습니다.
        </p>
      </div>

      <div class="cta-section">
        <a href="/blog" class="cta-button">블로그 글 보러가기 →</a>
      </div>
    </main>
    <Footer />
  </body>
</html>

<style>
  .home-main {
    padding: 0;
    max-width: 100%;
  }

  .hero-section {
    position: relative;
    width: 100%;
    height: 500px;
    overflow: hidden;
    margin-bottom: 4rem;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.3),
      rgba(0, 0, 0, 0.6)
    );
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
    padding: 2rem;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    margin: 0 0 1rem 0;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
  }

  .hero-subtitle {
    font-size: 1.5rem;
    font-weight: 400;
    margin: 0;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
  }

  .intro-section {
    max-width: 65ch;
    margin: 0 auto 4rem;
    padding: 0 2rem;
  }

  .intro-section h2 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: var(--text-color);
  }

  .intro-section p {
    font-size: 1.125rem;
    line-height: 1.8;
    margin-bottom: 1.5rem;
    color: var(--text-color);
  }

  .cta-section {
    text-align: center;
    margin-bottom: 4rem;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2.5rem;
    background-color: var(--accent-color);
    color: white;
    font-size: 1.125rem;
    font-weight: 600;
    border-radius: 0.5rem;
    text-decoration: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    opacity: 0.9;
    text-decoration: none;
  }

  @media (max-width: 768px) {
    .hero-section {
      height: 350px;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }

    .intro-section {
      padding: 0 1.5rem;
    }
  }
</style>
